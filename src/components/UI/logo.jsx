/* import { chakra } from '@chakra-ui/react'
import NextLink from 'next/link'

const Logo = ({ colorMode }) => (
  <>
    <NextLink href="/">
      <chakra.a
        style={colorMode === 'light' ? { color: 'white' } : { color: 'black' }}
        href="/"
        userSelect="none"
        fontSize="2rem"
        fontWeight="700"
      >
        World Almanac
      </chakra.a>
    </NextLink>
  </>
)

export default Logo
 */
